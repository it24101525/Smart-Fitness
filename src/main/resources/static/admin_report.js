// src/main/resources/static/admin_report.js

document.addEventListener('DOMContentLoaded', function() {
    // Example: You can add code here to fetch and display stats dynamically if needed.
    // For now, the backend renders all stats on page load.
    // If you want to refresh stats via AJAX:
    /*
    fetch('/admin/api/statistics')
        .then(res => res.json())
        .then(stats => {
            // Update DOM with stats
        });
    */
});